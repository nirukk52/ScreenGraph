{
  "name": "screengraph",
  "private": true,
  "scripts": {
    "dev": "concurrently -n backend,frontend -c blue,green \"cd backend && encore run\" \"cd frontend && bun run dev\"",
    "dev:backend": "cd backend && encore run",
    "dev:frontend": "cd frontend && bun run dev"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  },
  "packageManager": "bun"
}
