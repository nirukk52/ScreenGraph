#!/bin/sh
# Husky post-checkout hook
# No worktree-specific automation required (single environment policy)
echo "ℹ️ Post-checkout: no worktree preflight required. Remember: tests run on main using standard ports."

