Keep this doc max two pages. This is a pre specify command phase where. We will brainstorm the command for @specify.specify here.

The command to /specify:
2-3 lines:

db rows updated created:

new dto created:

Affected folder structure with filenames draw it:

The command to /specify:
2-3 lines.