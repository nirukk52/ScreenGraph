import { Service } from "encore.dev/service";

/**
 * AppInfo Service
 * PURPOSE: Hosts Encore endpoints for ingesting and retrieving Play Store
 * metadata persisted in the ScreenGraph backend database.
 */
export default new Service("appinfo");

