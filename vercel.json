{
  "buildCommand": "cd frontend && rm -rf .svelte-kit .vercel && bun install && bun run build",
  "outputDirectory": "frontend/.vercel/output",
  "devCommand": "cd frontend && bun run dev",
  "installCommand": "bun install",
  "framework": "sveltekit"
}

