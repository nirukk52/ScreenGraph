#!/usr/bin/env bash
# Validate Docs Command - Check all FR/BUG/TD/CHORE items
#
# Validates:
# - Required files exist (handoff.md, main.md, status.md)
# - Line limits enforced (handoff ≤50, main ≤150, status ≤100)
# - Valid todo format
# - Manual testing section present

set -euo pipefail

cd "$(dirname "$0")/../.."
node automation/scripts/validate-cursor-docs.mjs "$@"

