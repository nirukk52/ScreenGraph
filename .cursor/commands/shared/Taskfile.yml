version: '3'

# Shared utilities used by all commands
# Environment helpers (single .env policy)

tasks:
  preflight:
    desc: "Load environment from .env"
    cmds:
      - echo "🔄 Loading .env (single-environment policy)"
      - node ../../../automation/scripts/env.mjs print
    silent: false

  load-env:
    desc: "Load and display environment variables"
    cmds:
      - node ../../../automation/scripts/env.mjs print
    silent: false

  status:
    desc: "Show environment status (static ports from .env)"
    cmds:
      - node ../../../automation/scripts/env.mjs status
    silent: false

  validate-commit-message:
    desc: "Validate commit message format (conventional commits)"
    cmds:
      - echo "📝 Validating commit message..."
      - echo "⚠️  Commit message validator not yet implemented"
      - echo "   This will check for type(scope) message format"
    silent: false

