/** Centralizes the fallback app configuration used when starting a run from the UI. */
export const DEFAULT_APK_PATH = "/Users/priyankalalge/SAAS/Scoreboard/AppiumPythonClient/test/apps/kotlinconf.apk";

/** Identifies the Android package we default to when no override is provided. */
export const DEFAULT_PACKAGE_NAME = "com.jetbrains.kotlinconf";

/** Declares the launchable app activity pattern used for the default demo flow. */
export const DEFAULT_APP_ACTIVITY = ".*";

/** Provides the default Appium server endpoint leveraged by the connect workflow. */
export const DEFAULT_APPIUM_SERVER_URL = "http://127.0.0.1:4723/";


